break : stops the loop but allows the execution of rest of code in the method 

continue : if(a==0){
    continue;
}      the current iteration is skipped but continues with next iteration 

return : exits the loop and the method entirely 


  